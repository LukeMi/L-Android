<resources>

    <style name="MenuStyle">
        <item name="android:windowEnterAnimation">@anim/anim_menu_scale_by_right_top_enter</item>
        <item name="android:windowExitAnimation">@anim/anim_menu_scale_by_right_top_exit</item>
    </style>

    <style name="Popup_bottom">
        <item name="android:windowEnterAnimation">@anim/slide_bottom_in</item>
        <item name="android:windowExitAnimation">@anim/slide_bottom_out</item>
    </style>

    <style name="Anim_Left_out_Right_int">
        <item name="android:windowEnterAnimation">@anim/anim_right_in</item>
        <item name="android:windowExitAnimation">@anim/anim_left_out</item>
    </style>

    <style name="BaseDialog" parent="android:Theme.Dialog">

        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 浮于Activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- Dialog以外的区域模糊效果 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 无标题 -->
        <item name="windowNoTitle">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">true</item>

    </style>

    <style name="DialogActivity" parent="Theme.AppCompat.Dialog">

        <item name="android:windowEnterAnimation">@anim/dd_mask_in</item>
        <item name="android:windowExitAnimation">@anim/dd_mask_out</item>
        <!--动画设置-->
        <!--        <item name="android:windowAnimationStyle">@null</item>-->
        <!--是否有覆盖-->
        <item name="android:windowContentOverlay">@null</item>
        <!--边框-->
        <item name="android:windowFrame">@null</item>
        浮于activity之上
        <item name="android:windowIsFloating">true</item>
        <!--是否半透明-->
        <item name="android:windowIsTranslucent">true</item>
        <!--无标题-->
        <item name="windowNoTitle">true</item>
        <!--背景透明-->
        <item name="android:windowBackground">@color/transparent</item>
        <!--dialog以外区域模糊效果-->
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <style name="AnimMaskIn">
        <item name="android:windowEnterAnimation">@anim/dd_mask_in</item>
        <item name="android:windowExitAnimation">@anim/dd_mask_out</item>
    </style>

    <style name="CustomBottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/CustomBottomSheetStyle</item>
    </style>

    <style name="CustomBottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="BottomSheetTansparentDialog" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="android:windowCloseOnTouchOutside">false</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:colorBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.3</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
    </style>

    <style name="bottom_navigation_view_active_size">
        <item name="android:textSize">10sp</item>
    </style>

    <style name="bottom_navigation_view_in_active_size">
        <item name="android:textSize">10sp</item>
    </style>
</resources>
